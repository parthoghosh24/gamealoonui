GamealoonUI README
------------------

This application is the browser based application with which users will be interacting when they are interacting with 
gamealoon via web browser. Using python to build this just for the purpose of faster development and also learning purpose.
Architecture for this application is simple. There is a view layer and then there is a controller layer which hits the gamealoon
api web service to read/write data

